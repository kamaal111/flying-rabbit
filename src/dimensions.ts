import { Dimensions } from 'react-native';

export const screenWidth: number = Math.round(Dimensions.get('window').width);
export const screenHeight: number = Math.round(Dimensions.get('window').height);
